# 数据结构概述
  ## 数据
    数据:信息的载体
	数据的基本单位:数据原数(记录)，由若干数据项构成，数据项是数据最小单位
  ## 抽象数据类型
	也叫ADT
	可以用类结构体的方式写
	通过抽象数据类型引入数据结构
	抽象数据类型包含数据/属性+函数/动作
  ## 数据结构(理解)
    就是数据之间相互的关系
    严:相互之间存在一种或多种特定关系的数据元素的集合。
  ## 逻辑结构
    线性结构
  ## 存储结构
	 理论上来说 就是用计算机语言实现的逻辑结构，存储时存储器中也相邻
	 